# Houston Intelligence Platform Requirements - Optimized for Railway
# This file uses CPU-only versions of PyTorch to reduce package size

# Core dependencies
pandas>=1.3.0
numpy>=1.21.0

# API Framework
flask>=2.0.0
flask-cors>=3.0.10
flask-limiter>=2.1
flask-caching>=1.10.1

# Production server
gunicorn>=20.1.0

# Data visualization (for chart generation)
plotly>=5.0.0
kaleido>=0.2.1

# JSON handling
simplejson>=3.17.0

# Utilities
python-dateutil>=2.8.0
pytz>=2021.3

# Environment management
python-dotenv>=0.19.0

# Machine Learning for knowledge base search
scikit-learn>=1.0.0

# For API integration
requests>=2.26.0

# Hugging Face AI Models - CPU versions
# Install torch CPU version explicitly
--index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
# Then install transformers
transformers>=4.36.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional: Smaller transformer models
# Consider using these instead of full transformers:
# sentence-transformers>=2.2.2  # For embeddings only
# tokenizers>=0.15.0  # Faster tokenization